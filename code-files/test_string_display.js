// Test string output to character display
// IO_CHARS_DISPLAY_WRITE at memory address 240-249

let greeting = "Hello";

// Write each character to display
for (let i = 0; i < greeting.length; i++) {
  let ch = greeting[i];
  // TODO: Write ch to IO_CHARS_DISPLAY_WRITE[i] (address 240 + i)
}
