// Test const with arrays

const size = 5;
let arr = [1, 2, 3, 4, 5];

// Use const in array access
let first = arr[0];
let last = arr[size - 1];

// Use const in calculations
let sum = first + last;
let doubled = sum + sum;
