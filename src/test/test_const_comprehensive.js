// Comprehensive const test

// Constants are stored in memory
const MAX = 100;
const MIN = 0;

// Let variables use registers
let counter = MIN;
let target = MAX;

// Use const in expressions
let range = target - counter;

// Const can be used multiple times
let sum = MAX + MIN;
